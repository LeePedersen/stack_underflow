<h1>Edit answer:</h1>

<%= form_for [@question, @answer] do |f| %>
<%= f.label :user_name %>
<%= f.text_field :user_name %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <p><%= f.submit %></p>
<% end %>
